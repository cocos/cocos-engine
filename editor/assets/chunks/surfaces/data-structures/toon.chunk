struct SurfacesMaterialData
{
  HIGHP_VALUE_STRUCT_DEFINE(vec3, worldPos);

  vec4 baseColor;
  vec4 specular;
  vec3 worldNormal;
  vec3 emissive;

  vec3 shade1;
  vec3 shade2;
  float baseStep;
  float baseFeather;
  float shadeStep;
  float shadeFeather;
  float shadowCover;
};
