
struct SpriteVertexData {
  vec4 position; // world position
  vec4 color; // vertex color
  vec2 uv; // uv
};

struct SpriteFragmentData {
  vec2 uv; // sampling UV
  vec4 color; // vertex color
};
