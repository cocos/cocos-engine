import { _decorator, Component, Node, RenderPipeline } from "cc";
const { ccclass, property } = _decorator;

@ccclass("<%Name%>")
export class <%Name%> extends RenderPipeline {

    /* use `property` decorator if your want the member to be serializable */
    // @property
    // serializableDummy = 0;

    destroy () {
    }
}
