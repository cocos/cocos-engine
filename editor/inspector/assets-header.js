'use strict';

const { join } = require('path');

module.exports = {
    'gltf-material': join(__dirname, './assets/material-header.js'), // 复用
    'material': join(__dirname, './assets/material-header.js'),
};
