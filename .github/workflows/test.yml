name: test

#on: push
on: [pull_request_target]

jobs:
  Check_pull_request_version:
    runs-on: ubuntu-latest
    steps:          
      - name: test token
        run: |
          echo ${{ secrets.LABEl_TOKEN }}
          echo ${{ secrets.GITHUB_TOKEN }}
      - uses: marocchino/sticky-pull-request-comment@v2
        with:
          message: |
            hello comment
