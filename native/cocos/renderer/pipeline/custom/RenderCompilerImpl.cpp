#include "RenderCompilerGraphs.h"

namespace cc {

namespace render {

int RenderCompiler::compile() {
    auto&       rg      = graph;
    const auto& resg    = resourceGraph;

    return 0;
}

} // namespace render

} // namespace cc
