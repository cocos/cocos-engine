#include "RenderCompilerGraphs.h"

namespace cc {

namespace render {

int RenderCompiler::compile() {
    auto*       scratch = mScratch;
    auto&       rg      = mGraph;
    const auto& resg    = mResourceGraph;

    return 0;
}

} // namespace render

} // namespace cc
