#include "../Core/CoreStd.h"
#include "Vec2.h"

CC_NAMESPACE_BEGIN

const Vec2i VEC2I_ZERO(0, 0);
const Vec2i VEC2I_ONE(1, 1);
const Vec2i VEC2I_NEG_ONE(-1, -1);
const Vec2i VEC2I_UNIT_X(1, 0);
const Vec2i VEC2I_UNIT_Y(0, 1);
const Vec2i VEC2I_NEG_UNIT_X(-1, 0);
const Vec2i VEC2I_NEG_UNIT_Y(0, -1);

const Vec2ui VEC2UI_ZERO(0, 0);
const Vec2ui VEC2UI_ONE(1, 1);
const Vec2ui VEC2UI_UNIT_X(1, 0);
const Vec2ui VEC2UI_UNIT_Y(0, 1);

const Vec2f VEC2F_ZERO(0.0f, 0.0f);
const Vec2f VEC2F_ONE(1.0f, 1.0f);
const Vec2f VEC2F_NEG_ONE(-1.0f, -1.0f);
const Vec2f VEC2F_UNIT_X(1.0f, 0.0f);
const Vec2f VEC2F_UNIT_Y(0.0f, 1.0f);
const Vec2f VEC2F_NEG_UNIT_X(-1.0f, 0.0f);
const Vec2f VEC2F_NEG_UNIT_Y(0.0f, -1.0f);
const Vec2f VEC2F_INVALID(Math::MAX_FLOAT, Math::MAX_FLOAT);

const Vec2d VEC2D_ZERO(0.0, 0.0);
const Vec2d VEC2D_ONE(1.0, 1.0);
const Vec2d VEC2D_NEG_ONE(-1.0, -1.0);
const Vec2d VEC2D_UNIT_X(1.0, 0.0);
const Vec2d VEC2D_UNIT_Y(0.0, 1.0);
const Vec2d VEC2D_NEG_UNIT_X(-1.0, 0.0);
const Vec2d VEC2D_NEG_UNIT_Y(0.0, -1.0);
const Vec2d VEC2D_INVALID(Math::MAX_DOUBLE, Math::MAX_DOUBLE);

CC_NAMESPACE_END