export default {
    layers: [{
        graph: {
            nodes: [{
                name: 'asd',
                type: 'animation',
            }],
            entryTransitions: [{
                to: 0,
            }],
        }
    }],
} as import('../../../cocos/core/animation/marionette/__tmp__/graph-description').GraphDescription;